import {BrowserRouter, Routes, Route} from 'react-router-dom';

function App() {
    return (
        <BrowserRouter>
        <Routes>
            <Route path='/Docentes' element={pass}>

            </Route>
        </Routes>
        </BrowserRouter>
    )
}